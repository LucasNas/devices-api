meta {
  name: POST-create-device
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/devices
  body: json
  auth: inherit
}

body:json {
  {
      "name": "IPhone",
      "brand": "Apple",
      "state": "AVAILABLE"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
